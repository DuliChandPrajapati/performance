import styled from 'styled-components';

import NormalImg from 'components/Img';

const Img = styled(NormalImg)`
  width: auto;
  height: auto;
  border: none;
  display: block;
`;

export default Img;
